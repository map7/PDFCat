- form_for @category do |f|
  %p
    = f.label :parent_id
    = f.collection_select :parent_id, @category.roots_without_self, :id, :name, :include_blank => true 
  %p
    = f.label :name
    = f.text_field :name
  %p
    = f.label :description
    = f.text_field :description
  = f.submit

= link_to 'Back', categories_path, :class=> 'ajax', :accesskey=> 'esc'
